name = "Rameshwaram"  # name is of string datatype, strings in python are immutable that means once created its value cannot be changed

nameshort = name[0:3]  # start from index 0 all the way to index 3 (excluding 3)
print(nameshort)

# To access specific character by index
character1 = name[1]
print(character1)
