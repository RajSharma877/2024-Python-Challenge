a = (1, 2, 5, 6, False, "Raj", "Harsh")
# a[0] = 123 # We cannot perform updation in tuple because tuple is immutable
# print(type(a))
print(a)

# b = (1) # Python will considers it as integer if we have to make a tuple with single element
b = (1,)  # Add the trailing , so that it will be tuple
print(type(b))


